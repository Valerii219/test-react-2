const ContactListItem = ({addContact}) => {
  return (
    <>
      <ul>
              <li ></li>
              <button onClick={addContact}>Add contact</button>
      </ul>
    </>
  );
};

export default ContactListItem;
